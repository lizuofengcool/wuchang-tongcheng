<template>
    <view class="page">
        <view class="content">
            <text class="code">404</text>
            <text class="text">页面不存在</text>
            <view class="btn" @click="goHome">
                <text class="btn-text">返回首页</text>
            </view>
        </view>
    </view>
</template>

<script setup lang="uts">
    const goHome = () => {
        uni.reLaunch({ url: '/pages/index/index' })
    }
</script>

<style>
    .page {
        flex: 1;
        background-color: #F5F5F5;
    }

    .content {
        flex: 1;
        align-items: center;
        justify-content: center;
    }

    .code {
        font-size: 64px;
        color: #FF6B35;
        font-weight: 700;
        margin-bottom: 12px;
    }

    .text {
        font-size: 16px;
        color: #666;
        margin-bottom: 32px;
    }

    .btn {
        background-color: #FF6B35;
        border-radius: 24px;
        padding: 12px 32px;
        align-items: center;
    }

    .btn-text {
        font-size: 14px;
        color: #FFFFFF;
    }
</style>

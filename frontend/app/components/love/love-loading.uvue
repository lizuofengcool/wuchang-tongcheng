<template>
    <view class="loading" :style="{ paddingTop: paddingTop + 'px', paddingBottom: paddingBottom + 'px' }">
        <view class="heart">
            <text class="heart-icon">♥</text>
        </view>
        <text class="text">{{ text }}</text>
    </view>
</template>

<script setup lang="uts">
    withDefaults(defineProps<{
        text?: string
        paddingTop?: number
        paddingBottom?: number
    }>(), {
        text: '加载中...',
        paddingTop: 40,
        paddingBottom: 40
    } as any)
</script>

<style>
    .loading {
        align-items: center;
        justify-content: center;
    }

    .heart {
        margin-bottom: 8px;
    }

    .heart-icon {
        font-size: 28px;
        color: #FF6B35;
        opacity: 0.6;
    }

    .text {
        font-size: 13px;
        color: #999;
    }
</style>

# 修改项目端口避免冲突进度文档

## 基本信息
- 日期：2026-07-18
- 功能：修改项目端口避免冲突
- 状态：已完成

## 背景
用户反馈当前项目端口与 `D:\kaifa\wcdm.wang` 下的三个项目存在冲突，要求统一调整端口，并且通过端口配置文件管理，禁止硬编码到代码中。

## 已完成
1. 制定新端口方案并落地：
   - 后端 API：`8088`
   - 管理后台：`5177`
   - PC 门户：`3010`
   - H5 小程序：`5178`
   - 统一开发代理：`8099`
   - PostgreSQL：`5434`、Redis：`6380`、RabbitMQ：`5673`/`15673`、Elasticsearch：`9210`、MinIO：`9010`/`9011`、Nginx：`8098`/`8443`
2. 创建/更新项目根目录 `.env` 作为唯一端口配置源。
3. 修改 `docker-compose.yml` 与 `deploy/docker-compose.yml`，所有宿主机映射端口从 `.env` 读取。
4. 修改后端 `backend/configs/config.yaml` 与 `config.yaml.example` 端口与 `.env` 一致。
5. 修改前端配置：
   - `frontend/.env.development`：`VITE_PORT=5177`，`VITE_BACKEND_URL=http://localhost:8088`
   - `frontend/vite.config.js` 从环境变量读取端口与后端地址
   - `frontend/pc/.env.example`：`PORT=3010`，`BACKEND_URL=http://localhost:8088`
   - `frontend/pc/package.json` 移除固定端口
   - `frontend/pc/next.config.mjs` 默认后端地址改为 `8088`
   - `frontend/pc/src/lib/api.ts` 默认后端地址改为 `8088`
   - 新增 `frontend/miniapp/.env.development` / `.env.production`
   - `frontend/miniapp/vite.config.js` 从环境变量读取端口与后端地址
   - `frontend/miniapp/src/api/request.js` 使用 `import.meta.env.VITE_BACKEND_URL`
6. 更新 `start.bat` / `stop.bat`，从 `.env` 读取端口并启动/停止服务。
7. 更新 `deploy/proxy.js`，从 `.env` 读取端口与路由。
8. 清理代码中硬编码端口：
   - `backend/internal/pkg/storage/storage.go` 默认后端地址从 `WCTC_BACKEND_URL` / `WCTC_SERVER_PORT` 读取
   - 同步更新 `backend/internal/pkg/storage/minio.go` 对象名生成逻辑，修复高并发/快速连续调用下对象名重复的 flaky test
9. 更新 `project-import.json` 与 `README.md`、`frontend/pc/README.md`、`frontend/miniapp/README.md` 端口说明。
10. 验证：
    - `go test ./internal/pkg/storage/...` PASS
    - `go vet ./...` PASS
    - `docker-compose config` PASS
    - `docker-compose -f deploy/docker-compose.yml config` PASS
    - `node --check deploy/proxy.js` PASS
    - `node --check frontend/vite.config.js` PASS
    - `node --check frontend/miniapp/vite.config.js` PASS
    - `node --check frontend/pc/next.config.mjs` PASS

## 待完成
- 无（等待用户确认是否提交代码或进行下一步验证）。

## 问题
- 已解决：`deploy/proxy.js` 与 `start.bat` 端口已统一从 `.env` 读取。
- 已解决：`storage.go` 中 `http://localhost:8080` 硬编码已改为从环境变量读取。
- 已解决：MinIO 对象名生成在高频调用下可能出现重复，已加入随机串避免冲突。

## 下一步
请用户确认是否需要提交代码并推送，或进行额外验证（如启动服务实地测试）。

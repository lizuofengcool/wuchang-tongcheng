# 启动项目进度文档

## 基本信息
- 日期：2026-07-19
- 功能：启动五常同城项目（后端 + 管理后台 + PC门户 + H5小程序）
- 状态：已完成

## 背景
用户要求启动项目，启动前需明确所有端口避免与其他项目冲突。经检查：
- 本地已有 PostgreSQL 运行在 5432（密码 12345678，默认库 daoxiang_erp）
- 本地已有 Redis 运行在 6379（密码 redis）
- 项目 .env 配置的应用端口（8088/5177/3010/5178）均空闲，无冲突
- 用户确认：PG/Redis 这种公用服务不需要改端口，直接用本地 5432/6379

## 端口清单（本次实际使用）
### 应用服务（已启动）
| 服务 | 端口 | 进程 PID | 访问地址 |
|------|------|---------|---------|
| 后端 API（Go） | 8088 | 20280 (main) | http://localhost:8088 |
| 管理后台（Vue + Vite） | 5177 | 20056 (node) | http://localhost:5177 |
| PC 门户（Next.js） | 3010 | 21916 (node) | http://localhost:3010/pc |
| H5 小程序（uni-app） | 5178 | 21992 (node) | http://localhost:5178/h5/ |

### 基础设施（使用本地已有）
| 服务 | 端口 | 凭据 |
|------|------|------|
| PostgreSQL | 5432 | user=postgres, password=12345678, db=wuchang_tongcheng |
| Redis | 6379 | password=redis, db=0 |

## 已完成
1. 检查项目端口配置（.env / docker-compose.yml / config.yaml.example）
2. 检查本地端口占用情况，确认无冲突
3. 通过 AskUserQuestion 与用户确认：
   - 启动 4 个服务（后端 + 管理后台 + PC门户 + H5）
   - 使用本地已有 PG:5432 / Redis:6379
   - 后端启动时自动迁移表结构
4. 验证 wuchang_tongcheng 数据库已存在（无需创建）
5. 生成后端 backend/configs/config.yaml（连本地 5432/6379）
6. 启动后端服务：`go run cmd/server/main.go` → 监听 0.0.0.0:8088，健康检查 /health 返回 200
7. 安装前端依赖：
   - frontend：`npm install` 新增 61 个包
   - frontend/pc：依赖已就绪
   - frontend/miniapp：依赖已就绪
8. 启动管理后台：`npm run dev` → Vite v5.4.21 监听 5177
9. 启动 PC 门户：`PORT=3010 npm run dev` → Next.js 16.2.9 监听 3010
10. 启动 H5 小程序：`npm run dev:h5` → Vite v5.2.8 监听 5178
11. 端口监听验证：4 个端口全部 LISTENING 状态

## 待完成
无

## 问题
- PC门户启动时 Next.js 提示检测到多个 lockfile（项目根目录、frontend、frontend/pc 各有一份），自动选择项目根目录的 lockfile 作为 workspace root。此为警告不影响运行，如需消除可在 next.config.mjs 配置 `turbopack.root`。
- RabbitMQ / Elasticsearch / MinIO 未启动，后端按设计自动降级运行，不影响主功能。

## 下一步
- 服务已全部运行，可直接访问对应地址使用。
- 如需停止服务：可执行 `stop.bat` 或手动结束对应 PID（20280 / 20056 / 21916 / 21992）。
- 后端日志输出在终端窗口；前端日志在各自终端窗口。

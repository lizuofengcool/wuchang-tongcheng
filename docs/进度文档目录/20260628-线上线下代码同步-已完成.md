# 线上线下代码同步进度文档

## 基本信息
- 日期：2026-06-28
- 功能：线上线下代码同步
- 状态：已完成

## 背景
线上 AI 与本地同时开发，需同步远程更新（23 个新提交，含 WebSocket、Redis 缓存、七牛云存储、MinIO、Elasticsearch、RabbitMQ、PC 门户 Next.js、小程序 uni-app、限流、单元测试等）且不丢失本地改动。

## 同步过程发现的问题
执行 `git stash -u` + `git pull` 后，发现 stash 中仅保存了本地**未跟踪文件**（shop/、groupbuy/、modules.go），本地对**已跟踪文件的修改**（Pluggable 接口、init() 自动注册、main.go 改造、五常地区数据、近享同城改名等）未能进入 stash，已丢失。需手动重新应用。

## 已完成

### 1. 远程更新拉取
- `git pull origin master` 快进合并，从 c6a5864 更新到 1279e9b（23 个提交，+22693 行）
- 远程新增：WebSocket、Redis 缓存、RabbitMQ、Elasticsearch、七牛云 Kodo、MinIO、PC 门户(Next.js)、小程序(uni-app)、限流中间件、单元测试

### 2. 本地未跟踪文件恢复
- 从 stash 未跟踪树（stash@{0}^3）恢复：shop/、groupbuy/、modules.go
- 丢弃 stash（其跟踪改动为远程副本，无用）

### 3. 重新应用丢失的跟踪修改
- **core/plugin/plugin.go**：添加 Pluggable 接口、AutoRegister()、MustRegister()、ListPluggable()、Context.DefaultQuery()
- **core/router/router.go**：添加 Context.DefaultQuery() 方法实现
- **7 个模块 plugin.go**（region/setting/file/permission/user/category/news）：添加 init() 自动注册
- **cmd/server/main.go**：移除 7 个手动 Register 调用，改用 `_ "wuchang-tongcheng/internal/modules"` 自动注册；Redis 从 Fatal 改为 Warn（允许无 Redis 启动）；保留远程的 Redis/MQ/WebSocket/ES/地图初始化
- **core/middleware/region.go**：DefaultRegionID = 3（五常市）
- **pkg/seed/seed.go**：地区数据改为 黑龙江省→哈尔滨市→五常市→4乡镇；admin 用户 RegionID=3；添加 shop/groupbuy 权限码
- **pkg/utils/error_code.go**：添加 shop 错误码(2500-2599)、groupbuy 错误码(2600-2699)
- **前端 8 个文件**：武昌同城→近享同城（index.html、.env.development、.env.production、MainLayout.vue、login.vue、dashboard.vue、router/index.js、stores/region.js）
- **stores/region.js**：DEFAULT_REGION_ID = 3

### 4. 编译验证
- 后端编译通过，生成 wuchang-tongcheng.exe
- 服务启动成功，监听 0.0.0.0:8080
- 9 个模块全部注册（region/setting/file/permission/user/category/news/shop/groupbuy）
- 健康检查正常
- 根路由返回"近享同城本地生活服务平台"
- 登录正常（admin/admin123）
- 地区树正确：黑龙江省(id=1)→哈尔滨市(id=2)→五常市(id=3)→4乡镇(id=4-7)

## 待完成
- 前端管理后台开发：商家管理页面、团购管理页面、模块管理页面
- 小程序(miniapp)和 PC 门户(pc)的品牌名同步（目前仍为"武昌/五常"相关字样）
- 提交代码到本地仓库

## 问题
- stash 机制未能保存已跟踪文件修改的原因未完全明确（可能与之前会话中断时的 git 操作顺序有关）
- 小程序和 PC 门户是远程新增的前端项目，其中品牌名仍为旧称，需后续统一

## 下一步
- 提交本次同步改动到本地 git
- 开发商家管理、团购管理前端页面
- 统一 miniapp/pc 品牌名为"近享同城"
